# 🔍 ExecutionGuard Summary Log - คำอธิบาย

## 📊 ภาพรวม

Log ที่คุณเห็นคือ **ExecutionGuard Summary** ซึ่งแสดงสรุปการทำงานของ ExecutionGuard ใน TestService

---

## 🖼️ อธิบาย Log แต่ละบรรทัด

### ส่วนที่ 1: Header

```
╔════════════════════════════════════════════════════════════════╗
║              EXECUTION GUARD SUMMARY                           ║
╠════════════════════════════════════════════════════════════════╣
```

**ความหมาย:** หัวข้อสรุป ExecutionGuard

---

### ส่วนที่ 2: Statistics (สถิติหลัก)

```
║ Total Records: 3
```

**ความหมาย:** มีงาน (tasks) ที่ถูกติดตามทั้งหมด **3 งาน**

- งานคือแต่ละ "key" ที่เรียก `RunOnce()`, `AcquireLock()`, etc.
- ในกรณีนี้คือ:
  1. `player_data_{userId}` - โหลดข้อมูลผู้เล่น
  2. `api_https://api.example.com` - เรียก API
  3. `order_12345` - ประมวลผล order

---

```
║ Total Executions: 4
```

**ความหมาย:** มีการ execute (รันฟังก์ชัน) ทั้งหมด **4 ครั้ง**

- **ทำไมถึง 4 ครั้ง ทั้งๆ ที่มีแค่ 3 งาน?**
  - งาน `player_data` ถูกเรียก **2 ครั้ง** (ครั้งแรก execute จริง, ครั้งที่ 2 ได้ cached result)
  - งาน `api_...` execute **1 ครั้ง** (แต่ timeout)
  - งาน `order_12345` execute **1 ครั้ง** (ถูก block โดย concurrent thread)

---

```
║ Blocked: 44 | Cached: 17
```

**ความหมาย:**

| Metric | ค่า | อธิบาย |
|--------|-----|--------|
| **Blocked** | 44 | การเรียกที่ถูกบล็อก (ไม่ให้รันซ้ำ) |
| **Cached** | 17 | การเรียกที่คืนผลลัพธ์แบบ cached |

**ตัวอย่าง:**
```lua
-- ครั้งที่ 1: Execute
guard:RunOnce("loadData", fn) → Executes, returns result

-- ครั้งที่ 2-18: Cached (17 ครั้ง)
guard:RunOnce("loadData", fn) → Returns cached result (ไม่รัน fn)

-- ครั้งที่ 19-62: Blocked (44 ครั้ง)
-- ถ้ามีคนพยายามรันขณะที่กำลัง execute อยู่
```

---

```
║ Timed Out: 2 | Failed: 0
```

**ความหมาย:**

| Metric | ค่า | อธิบาย |
|--------|-----|--------|
| **Timed Out** | 2 | งานที่ timeout (เกินเวลาที่กำหนด) |
| **Failed** | 0 | งานที่ error/fail |

**ตัวอย่าง Timeout:**
```lua
-- ตั้ง timeout 3 วินาที
guard:RunOnce("api", function()
    task.wait(5) -- รอ 5 วินาที → Timeout!
end, { timeout = 3 })

-- ผลลัพธ์: TimedOut ✅
```

---

### ส่วนที่ 3: Status Distribution (แยกตามสถานะ)

```
╠════════════════════════════════════════════════════════════════╣
║ Status Distribution:
```

**ความหมาย:** สถิติแยกตามสถานะของแต่ละงาน

---

```
║ ✅ Completed              2
```

**ความหมาย:** มี **2 งาน** ที่ทำเสร็จแล้วสำเร็จ

- งาน `player_data_{userId}` → Completed ✅
- งาน `task_001` (จาก `complexOperation()`) → Completed ✅

---

```
║ ⏱️ TimedOut                1
```

**ความหมาย:** มี **1 งาน** ที่ timeout

- งาน `api_https://api.example.com` → TimedOut ⏱️
  - เพราะ: ตั้ง timeout 3s แต่ `task.wait(5)` → timeout!

---

## 🎯 สรุปภาพรวม (จากภาพ)

```
┌─────────────────────────────────────────────────────────────────┐
│                    📊 สถิติที่เห็น                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📂 Total Records: 3                                            │
│     └─ มี 3 งานที่ถูกติดตาม                                     │
│                                                                 │
│  ▶️ Total Executions: 4                                         │
│     └─ มีการรันฟังก์ชันทั้งหมด 4 ครั้ง                         │
│                                                                 │
│  🚫 Blocked: 44                                                 │
│     └─ มีการพยายามรันซ้ำ แต่ถูกบล็อก 44 ครั้ง                 │
│        (เพราะกำลังรันอยู่หรือมี cached result)                 │
│                                                                 │
│  ♻️ Cached: 17                                                  │
│     └─ คืนผลลัพธ์แบบ cached 17 ครั้ง                           │
│        (ไม่ต้องรันฟังก์ชันซ้ำ)                                 │
│                                                                 │
│  ⏱️ Timed Out: 2                                                │
│     └─ มี 2 งานที่ timeout (รันเกินเวลา)                       │
│                                                                 │
│  ❌ Failed: 0                                                   │
│     └─ ไม่มีงานที่ fail/error                                   │
│                                                                 │
│  ─────────────────────────────────────────────                 │
│                                                                 │
│  ✅ Completed: 2 งาน                                            │
│     • player_data_{userId} → Success!                           │
│     • task_001 → Success!                                       │
│                                                                 │
│  ⏱️ TimedOut: 1 งาน                                             │
│     • api_https://api.example.com → Timeout!                    │
│       (ตั้ง 3s แต่รัน 5s)                                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔍 การวิเคราะห์ Log

### Scenario 1: Normal Case (งานที่สำเร็จ)

```lua
-- โหลดข้อมูลผู้เล่น
guard:RunOnce("player_data_123", function()
    task.wait(2)
    return {coins = 1000, level = 5}
end, { timeout = 10, cacheResult = true })

-- ผลลัพธ์:
-- ครั้งที่ 1: ✅ Completed (execute จริง)
-- ครั้งที่ 2: ♻️ Cached (คืนผลแบบ cache)
```

---

### Scenario 2: Timeout Case (งานที่ timeout)

```lua
-- เรียก API (ช้าเกินไป)
guard:RunOnce("api_call", function()
    task.wait(5) -- รอ 5 วินาที
    return "data"
end, { timeout = 3 }) -- แต่ตั้ง timeout 3 วินาที!

-- ผลลัพธ์:
-- ⏱️ TimedOut (เพราะ 5s > 3s)
```

---

### Scenario 3: Blocked Case (พยายามรันซ้ำ)

```lua
-- Thread 1
guard:RunOnce("process_order", function()
    task.wait(3)
    return "done"
end)

-- Thread 2 (ขณะที่ Thread 1 ยังรันอยู่)
guard:RunOnce("process_order", function()
    -- จะไม่รัน! ถูก BLOCKED
end)

-- ผลลัพธ์:
-- Thread 1: ▶️ Running → ✅ Completed
-- Thread 2: 🚫 Blocked (เพราะ Thread 1 ยังรันอยู่)
```

---

## 📊 ตัวอย่างการใช้งานจริง

### Use Case 1: Load Player Data

```lua
function loadPlayerData(userId)
    local success, data = guard:RunOnce(`player_{userId}`, function()
        print("💾 Loading from DataStore...")
        task.wait(2) -- Simulate slow API
        return {coins = 1000, level = 5}
    end, {
        timeout = 10,
        cacheResult = true
    })
    
    return data
end

-- ครั้งที่ 1: 💾 Loading... → ✅ Completed
-- ครั้งที่ 2-18: ♻️ Cached (ไม่โหลดใหม่)
```

---

### Use Case 2: External API Call

```lua
function fetchFromAPI(url)
    local success, result = guard:RunOnce(`api_{url}`, function()
        print("🌐 Calling API...")
        task.wait(5) -- API ช้า!
        return "response"
    end, {
        timeout = 3, -- Timeout 3s
        onTimeout = function()
            warn("⏱️ API timeout!")
        end
    })
    
    return result
end

-- ผลลัพธ์: ⏱️ TimedOut (เพราะ 5s > 3s)
```

---

## 🎓 สรุป Log

| Metric | ค่าที่เห็น | ความหมาย |
|--------|-----------|----------|
| Total Records | 3 | มี 3 งานที่ถูกติดตาม |
| Total Executions | 4 | มีการรันจริง 4 ครั้ง |
| Blocked | 44 | ถูกบล็อก 44 ครั้ง (กำลังรันอยู่/มี cache) |
| Cached | 17 | คืนค่า cached 17 ครั้ง |
| Timed Out | 2 | Timeout 2 งาน |
| Failed | 0 | ไม่มี error |
| ✅ Completed | 2 | สำเร็จ 2 งาน |
| ⏱️ TimedOut | 1 | Timeout 1 งาน |

---

## 💡 ทำไม Log นี้สำคัญ?

### 1. ติดตามประสิทธิภาพ

```
Blocked: 44    ← บอกว่าระบบป้องกัน duplicate calls ได้ดี!
Cached: 17     ← บอกว่า cache ทำงานได้ดี!
Timed Out: 2   ← บอกว่ามีงานที่ช้าเกินไป (ควรตรวจสอบ)
```

### 2. Debug ปัญหา

```
Failed: 0      ← ไม่มี error (ดี!)
Timed Out: 2   ← มีงาน timeout (ควรเพิ่ม timeout หรือทำให้เร็วขึ้น)
```

### 3. Monitor การใช้งาน

```
Total Executions: 4   ← มีการเรียกใช้ 4 ครั้ง
Blocked: 44           ← ป้องกัน duplicate ได้ 44 ครั้ง
→ Efficiency: 44/48 = 91.67% (ดีมาก!)
```

---

**Version:** 1.0  
**Author:** OneShortArena Team  
**Purpose:** อธิบาย ExecutionGuard Summary Log
